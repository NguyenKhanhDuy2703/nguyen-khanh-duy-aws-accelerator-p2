apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/NguyenKhanhDuy2703/nguyen-khanh-duy-aws-accelerator-p2.git'
    targetRevision: main
    path: CICD_repo/FE
    directory:
      include: "deployment.yaml,service.yaml,namespace.yaml"
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: fullstack-namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true