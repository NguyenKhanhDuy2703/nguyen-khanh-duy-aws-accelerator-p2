apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: cicd-demo
  annotations:
      argocd.argoproj.io/sync-wave: "1"

spec:
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
